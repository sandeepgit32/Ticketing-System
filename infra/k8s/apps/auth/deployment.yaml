apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticketing-system-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ticketing-system-auth
  template:
    metadata:
      labels:
        app: ticketing-system-auth
    spec:
      containers:
        - name: auth
          image: auth:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - secretRef:
                name: ticketing-system-credentials
            - configMapRef:
                name: ticketing-system-config
          env:
            - name: SERVICE_NAME
              value: auth
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 20
