apiVersion: v1
kind: Service
metadata:
  name: ticketing-system-redis
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: ticketing-system-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
