apiVersion: v1
kind: Service
metadata:
  name: ticketing-system-mysql
spec:
  type: NodePort
  selector:
    app: ticketing-system-mysql
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30060
