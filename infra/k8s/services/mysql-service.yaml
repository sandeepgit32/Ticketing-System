apiVersion: v1
kind: Service
metadata:
  name: ticketing-system-mysql
  namespace: app
spec:
  type: NodePort
  selector:
    app: ticketing-system-mysql
  ports:
    - name: mysql
      protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30307
