apiVersion: v1
kind: Service
metadata:
  name: ticketing-system-payment-mock
spec:
  type: ClusterIP
  selector:
    app: ticketing-system-payment-mock
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
