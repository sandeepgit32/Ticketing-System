apiVersion: v1
kind: Service
metadata:
  name: ticketing-system-booking
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: ticketing-system-booking
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
